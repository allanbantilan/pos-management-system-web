[deploy]
startCommand = "if [ \"$MIGRATE_ON_START\" = \"true\" ]; then php artisan migrate --force; fi && php artisan config:cache && php artisan route:cache && php artisan view:cache && php artisan serve --host=0.0.0.0 --port=$PORT"
